# NOSTOS-0 dense tool workflow protocol

**Frozen:** 27 August 2026 before execution  
**Purpose:** software-level verification of the released `measure-series --dense` contract, not another scientific confirmation

Use BBBC035 plane 33 and fixed seed 9911 to create a two-frame series with a smooth programmed deformation. Execute the same application function used by the CLI with 0.1267-µm spatial spacing and 29-min temporal spacing. Pass only if the response is valid; all four dense response surfaces are present; time and both spatial coordinates are retained; displacement and uncertainty are finite; eligible fraction exceeds 0.60; source and generated-series hashes are recorded; and runtime is below 120 s on the audit workstation.

Passing verifies an end-to-end public-data file workflow. It does not establish native motion, tracking, mechanics or independent usability.
